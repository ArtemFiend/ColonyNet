# Local model directory

Place the two trusted model files here:

- `petri_detector_yolo26s_best.pt`
- `colony_yolo26x_seg_best.pt`

Model weights are intentionally excluded from Git. See the root README for
expected SHA-256 checksums and setup instructions.
